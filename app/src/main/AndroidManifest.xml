<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ldc.wandroidkt">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".mApp"
        android:allowBackup="false"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activitys.RegisterActivity" />
        <activity
            android:name=".ui.activitys.SearchArticleActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.MyFavoriteActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.IntegralRankActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.PersonalIntegralListActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.LoginActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.WebViewActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.SystemArticleActivity"
            android:launchMode="singleTop" />
        <activity
            android:name=".ui.activitys.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>